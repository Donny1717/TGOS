import { ProductPage } from "@/components/product-page";

export default function DCOSPage() {
  return <ProductPage code="DCOS" eyebrow="Real document control and intelligence" title="Give every document a trusted home." description="DCOS turns scattered files into a living intelligence repository—so people can find the right version, understand its context and act on it." capabilities={["Bring controlled documents, owners and versions into one repository.", "Surface useful context around the knowledge your teams rely on.", "Keep governance visible without slowing down the work."]} accent="bg-violet-300" />;
}
