import { ProductPage } from "@/components/product-page";

export default function TGOSPage() {
  return <ProductPage code="TGOS" eyebrow="Tender and document verification" title="Make every tender answer defensible." description="TGOS gives bid teams a source-first workspace to capture requirements, assign ownership and verify evidence before submission." capabilities={["Trace every answer back to its source and review state.", "See coverage, gaps and deadlines in one focused view.", "Create a clear, exportable record for confident handover."]} accent="bg-cyan-300" />;
}
