# Replaced by ci.yml — keep file so old links do not 404; workflow is disabled.
name: Node.js CI (legacy disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use workflow CI (ci.yml) instead."
